#include <iostream>
using namespace std;

int main(int argc, char *argv[]) {
	/*seekp si voy a escrbir, seekg si voy a leer;*/

//v[j] = x;   //planteado como un vector
//archi.seekp(j*sizeof(int));
//archi.write(reinterpret_cast<char*> &x, sizeof(x));

	
//x = v[i];
//archi.seekg(. . . .)
//archi.read(. . . .)

//usar una variable auxiliar primero
//int aux = v[i];
//v[i+1] = aux;
	
	
	return 0;
}

